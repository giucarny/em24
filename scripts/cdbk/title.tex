$if(title)$
\title{$title$}
$endif$

$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$,$endfor$]{$by-author.name.literal$}
$endfor$

$for(by-affiliation)$
\affil[$it.number$]{$if(it.name)$$it.name$$endif$$if(it.city)$, $it.city$$endif$$if(it.country)$, $it.country$$endif$}
$endfor$

\date{$date$}